 //data 1
 let markMass = 78
 let markHeight = 1.69
 let markBMI = markMass / (markHeight * markHeight)

 let joinMass = 92
 let joinHeight = 1.95
 let joinBMI = joinMass / (joinHeight * joinHeight)
 if (markBMI > joinBMI) {
     console.log("chỉ số BMI của mark là:" + markBMI + " cao hơn chỉ số BMI của join là" + joinBMI)
 } else {
     console.log("chỉ số BMI của mark là:" + markBMI + " thấp hơn chỉ số BMI của join là" + joinBMI)
 }



 //data 2
 let markMass2 = 95
 let markHeight2 = 1.88
 let markBMI2 = markMass2 / (markHeight2 * markHeight2)

 let joinMass2 = 85
 let joinHeight2 = 1.76
 let joinBMI2 = joinMass2 / (joinHeight2 * joinHeight2)
 if (markBMI2 > joinBMI2) {
     console.log("chỉ số BMI của mark data 2là:" + markBMI2 + " cao hơn chỉ số BMI của join là" + joinBMI2)
 } else {
     console.log("chỉ số BMI của mark data2 là:" + markBMI2 + " thấp hơn chỉ số BMI của join là" + joinBMI2)
 }